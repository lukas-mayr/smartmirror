{
  "name": "@mirror/core",
  "version": "0.22.0",
  "private": true,
  "type": "module",
  "description": "Server des Spiegels: Modul-Host, State-Bus, Konfiguration, Fernsteuerungs-API",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc --build",
    "start": "node dist/index.js",
    "dev": "node --watch dist/index.js",
    "clean": "tsc --build --clean"
  },
  "dependencies": {
    "@fastify/static": "^8.0.3",
    "@fastify/websocket": "^11.0.1",
    "@mirror/sdk": "0.22.0",
    "fastify": "^5.2.0"
  },
  "devDependencies": {
    "@types/ws": "^8.18.1"
  }
}
